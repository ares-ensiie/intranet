%h1 Nouvelle promotion

= simple_form_for [:admin, @promotion], html: { class: 'form-horizontal' } do |f|
  = f.input :name, :class => "form-control"
  .form-actions
    = f.submit class: 'btn btn-primary', confirm: "Êtes vous sur de créer une nouvelle promotion"
